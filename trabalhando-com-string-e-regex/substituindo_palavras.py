import re


texto = input('Digite o texto a ser revisado: ').split()
palavra_substituir = input('Digite a palavra que deseja substituir: ')
palavra_nova = input(f'Digite a palavra que irá substituir a palavra {palavra_substituir}: ')

lista = texto 

print(lista)